#include "main.h"

/**
 * _islower - checks lowercase or uppercase
 * @c: the character to check
 * Return: 1 if greater and 0 if less than
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
