#include "main.h"

/**
 * _abs - prints the absolute value of a number
 * @n: the number to check
 * Return: The absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
